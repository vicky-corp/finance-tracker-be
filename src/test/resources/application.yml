spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
#        format_sql: true
        show_sql: true